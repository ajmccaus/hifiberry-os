[Unit]
# The upstream unit has Restart=on-failure with StartLimitIntervalSec=0;
# on a device without any backlight (e.g. HDMI display) the daemon would
# fail and restart every 5 seconds forever. Only start when a backlight
# exists.
ConditionPathExistsGlob=/sys/class/backlight/*
