{
  "name": "beocreate-debian-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright tests for the Beocreate classic UI port (server + mock ACR + mock backlight)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
